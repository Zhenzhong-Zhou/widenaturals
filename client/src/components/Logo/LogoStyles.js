const logoStyles = (theme) => ({
    logoImage: {
        height: '35px',
        marginRight: '8px',
        transition: 'transform 0.3s ease-in-out',
        '&:hover': {
            transform: 'scale(1.1)',
        },
    },
});

export default logoStyles;